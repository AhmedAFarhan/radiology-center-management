@inject ResourceService ResourceService
@inject ISnackbar Snackbar

<MudDialog>
    <DialogContent>
        <MudText Typo="Typo.body2" Class="mb-2 text-secondary">
            Current status: <MudChip T="string" Size="Size.Small" Color="@StatusColor(Equipment.Status)">@FormatStatus(Equipment.Status)</MudChip>
        </MudText>
        <MudSelect T="string"
                   @bind-Value="_status"
                   Label="Status"
                   Variant="Variant.Outlined"
                   Margin="Margin.Dense"
                   Class="w-100">
            @foreach (var option in StatusOptions)
            {
                <MudSelectItem T="string" Value="@option.Key">@option.Value</MudSelectItem>
            }
        </MudSelect>
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Default" OnClick="CancelAsync" Disabled="_busy">Cancel</MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="SubmitAsync" Disabled="_busy">
            @if (_busy)
            {
                <MudProgressCircular Size="Size.Small" Indeterminate="true" Class="mx-1" />
            }
            else
            {
                <text>Save Status</text>
            }
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [Parameter] public EquipmentDto Equipment { get; set; } = default!;

    [CascadingParameter] public IMudDialogInstance MudDialog { get; set; } = default!;

    private static readonly Dictionary<string, string> StatusOptions = new()
    {
        ["Operational"] = "Operational",
        ["UnderMaintenance"] = "Under Maintenance",
        ["OutOfService"] = "Out of Service",
        ["Retired"] = "Retired",
    };

    private string _status = string.Empty;
    private bool _busy;

    protected override void OnInitialized()
        => _status = Equipment.Status;

    private async Task SubmitAsync()
    {
        if (string.IsNullOrWhiteSpace(_status))
        {
            Snackbar.Add("Select a status.", Severity.Warning);
            return;
        }

        _busy = true;
        try
        {
            await ResourceService.SetEquipmentStatusAsync(Equipment.Id, _status);
            Snackbar.Add("Equipment status updated.", Severity.Success);
            MudDialog.Close(DialogResult.Ok(true));
        }
        catch (ApiException ex)
        {
            Snackbar.Add(ex.Message, Severity.Error);
        }
        catch (Exception)
        {
            Snackbar.Add("Unable to reach the server. Please try again.", Severity.Error);
        }
        finally
        {
            _busy = false;
        }
    }

    private void CancelAsync()
        => MudDialog.Cancel();

    private static string FormatStatus(string status) => status switch
    {
        "UnderMaintenance" => "Under Maintenance",
        "OutOfService" => "Out of Service",
        _ => status,
    };

    private static Color StatusColor(string status) => status switch
    {
        "Operational" => Color.Success,
        "UnderMaintenance" => Color.Warning,
        "OutOfService" => Color.Error,
        _ => Color.Default,
    };
}
